apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: claimbot
type: Opaque
data:
  # Note: These are example values. In production, use proper secret management
  # The values should be base64 encoded
  OPENAI_API_KEY: YOUR_BASE64_ENCODED_API_KEY 